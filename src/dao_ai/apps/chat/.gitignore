node_modules/
dist/
server/public/
*.log
.env
.DS_Store

# The repo-root .gitignore has a Python-oriented `lib/` rule that would
# otherwise swallow the client's source `lib/` directory (contract/config/api).
# Re-include it for this app.
!client/src/lib/
!client/src/lib/**
